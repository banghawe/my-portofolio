name = "my-portofolio"
compatibility_date = "2024-01-01"

# Pages configuration
pages_build_output_dir = "dist"

[build]
command = "npm run build"

[build.upload]
format = "service-worker"

# Environment variables (if needed)
[env.production]
name = "my-portofolio"

[env.preview]
name = "my-portofolio-preview"
